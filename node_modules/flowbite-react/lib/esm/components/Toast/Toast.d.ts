import type { ComponentProps, FC, PropsWithChildren } from 'react';
import type { DeepPartial } from 'flowbite-react';
import type { Duration } from 'flowbite-react/lib/esm/components/Toast/ToastContext';
export interface FlowbiteToastTheme {
    root: {
        base: string;
        closed: string;
    };
    toggle: {
        base: string;
        icon: string;
    };
}
export interface ToastProps extends PropsWithChildren<ComponentProps<'div'>> {
    duration?: Duration;
    theme?: DeepPartial<FlowbiteToastTheme>;
}
export declare const Toast: FC<ToastProps> & {
    Toggle: FC<import("flowbite-react/lib/esm/components/Toast/ToastToggle").ToastToggleProps>;
};
